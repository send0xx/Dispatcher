version: 2

updates:
  # NuGet versions live in Directory.Packages.props, so every update lands in that one file.
  - package-ecosystem: nuget
    directory: /
    schedule:
      interval: weekly
    open-pull-requests-limit: 5
    groups:
      xunit:
        patterns:
          - xunit*
          - Microsoft.NET.Test.Sdk
    ignore:
      # Pinned to the lowest Roslyn version the generator supports, so it must be raised
      # deliberately rather than tracked to the newest release.
      - dependency-name: Microsoft.CodeAnalysis.CSharp
      # Deliberately multi-targeted: net8.0 stays on 8.0.x so the package keeps its
      # lowest supported Microsoft DI dependency. Dependabot ignores the TargetFramework
      # condition on the PackageVersion items, so it would raise the net8.0 entry too.
      - dependency-name: Microsoft.Extensions.DependencyInjection
      - dependency-name: Microsoft.Extensions.DependencyInjection.Abstractions

  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    open-pull-requests-limit: 5
